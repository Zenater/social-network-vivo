import React from 'react';

/*
export const updateObjectInArray = (items:any,userId:number,objPropName:any,newObjProps:any)=> {
    return items.map(u=> {
        if(u[objPropName]===userId) {
            return {...u,...newObjProps}
        }
        return u
    })
}*/
